import { StyleSheet } from 'react-native';

export const discoveryPaperOverrides = StyleSheet.create({
  v35JourneyProgress: {
    display: 'none',
  },
  v41ActiveFind: {
    display: 'none',
  },
});
