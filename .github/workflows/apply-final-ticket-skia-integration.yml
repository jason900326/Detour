name: Apply final ticket Skia integration

on:
  push:
    branches:
      - fix/final-ticket-skia-integration

permissions:
  contents: write

jobs:
  integrate-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test workflow
        run: echo "workflow-ok"
